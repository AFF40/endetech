import 'package:flutter/material.dart';

class TechnicianKardexScreen extends StatelessWidget {
  const TechnicianKardexScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Technician Kardex'),
      ),
      body: const Center(
        child: Text('Technician Kardex Screen'),
      ),
    );
  }
}
