import 'package:flutter/material.dart';

class MaintenancesReportScreen extends StatelessWidget {
  const MaintenancesReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Maintenances Report'),
      ),
      body: const Center(
        child: Text('Maintenances Report Screen'),
      ),
    );
  }
}
