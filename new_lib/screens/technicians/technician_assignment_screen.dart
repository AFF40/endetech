import 'package:flutter/material.dart';

class TechnicianAssignmentScreen extends StatelessWidget {
  const TechnicianAssignmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Assign Equipment/Tasks'),
      ),
      body: const Center(
        child: Text('Assign Equipment/Tasks Screen'),
      ),
    );
  }
}
